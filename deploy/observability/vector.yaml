# OpenGeoBot — Vector log pipeline configuration
# Time: 2026-07-03
# Author: AxeXie
sources:
  docker_logs:
    type: docker_logs

sinks:
  loki:
    type: loki
    inputs:
      - docker_logs
    endpoint: http://loki:3100
    labels:
      source: docker
